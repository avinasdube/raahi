# Vite environment variables must be prefixed with VITE_
# Base URL used by the client Axios for API calls.
# Defaults to http://localhost:8800/api in development and a production URL otherwise (see src/utils/index.js).
VITE_API_BASE_URL=http://localhost:8800/api
